node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment & secrets
.env
.env.local
.env.*.local

# Replit / editor state (jangan di-commit)
.local
.config
.replit

# Qodo — commit .qodo/workflows & best_practices; jangan commit cache user
.qodo/history

# Logs & cache
*.log
npm-debug.log*
.cache

# Staging aset revisi konten (ZIP client — jangan di-commit)
content-revision/incoming/
content-revision/previews/
